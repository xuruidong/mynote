#include "c1818_minAbsoluteSumDiff.h"

c1818_minAbsoluteSumDiff::c1818_minAbsoluteSumDiff()
{
    //ctor
}

c1818_minAbsoluteSumDiff::~c1818_minAbsoluteSumDiff()
{
    //dtor
}

int minAbsoluteSumDiff(vector<int>& nums1, vector<int>& nums2)
{
    /*
    vector<pair<int, int>> diff(nums1.size());
    for(int i=0; i<nums1.size(); ++i) {
        diff[i] = {abs(nums1[i]-nums2[i], i)};
    }
    sort(diff.begin(), diff.end());
    sort(nums1.begin(). nums1.end());

    for(auto it=diff.rbegin(); it != diff.rend(); ++it) {
        int ind = it->second;
        auto n1_it = lower_bound(nums1.begin(), nums1.end(), nums2[ind]);
        if(n1_it != nums1.)
    }
    */
}
